name: viperleed
channels:
  - defaults
dependencies:
  - python=3.11
  - numpy
  - scipy
  - wrapt
  - matplotlib
  - scikit-learn
  - psutil # on linux, may require sudo apt install pythonxx.xx-dev to build
  - line_profiler
  - pip
  - pip:
    - quicktions
    - fortranformat
    - pyqt5
    - mplcairo
