Bootstrap: docker
From: python

##ViperLEED test

%labels
Author Alexander M. Imre
Version v0.0.1

%setup
# nothing to go in here
%files
# potentially copy over files to the image


%post
apt-get update -y
apt-get install -y gcc

# install lapack, blas
apt-get install -y gfortran
apt-get install -y libblas-dev
apt-get install -y liblapack-dev

# install editior for convenience and debugging
apt-get install -y less
apt-get install -y nano
apt-get install -y vim

# install python packages
yes | pip install --upgrade pip
yes | pip install numpy scipy wrapt psutil quicktions sklearn matplotlib line-profiler

